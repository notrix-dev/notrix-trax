# RAG Example

Run:

```bash
python app.py
trax list
trax inspect <run_id>
trax replay <run_id>
trax explain <run_id>
```

This example uses `traced_retrieval` and `traced_chat` to generate a simple retrieval-plus-generation run.
