# Agent Example

Run:

```bash
python app.py
trax inspect <run_id>
trax explain <run_id>
```

This example creates a simple reasoning → retrieval → generation workflow with an empty retrieval result so detector and explanation output are visible.
